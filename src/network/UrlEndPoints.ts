const BASE_URL = "https://your-api-endpoint.com/";

const Endpoints = {
    DEVICE_DETAILS: `${BASE_URL}device-details`,
    PUBLIC_IP_ADDRESS: `https://api.ipify.org?format=json`,
}

export default Endpoints;